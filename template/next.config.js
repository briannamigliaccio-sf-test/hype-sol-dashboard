/** @type {import('next').NextConfig} */
const nextConfig = {
  // Uncomment if your data API is on a different origin:
  // async rewrites() {
  //   return [{ source: '/api/:path*', destination: 'https://your-api.com/:path*' }];
  // },
};
module.exports = nextConfig;
